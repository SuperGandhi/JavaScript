import * as modulesMathe from '../Exercise-eight/ModulesMath/controller.js'
import chalk from 'chalk';

const multi = modulesMathe.multiplica(1,2)
console.log(multi);

const sum = modulesMathe.suma(4,5);
console.log(chalk.green(sum));
